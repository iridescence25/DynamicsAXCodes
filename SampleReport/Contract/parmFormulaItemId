[
    DataMemberAttribute,
    SysOperationLabelAttribute(literalstr("@CUS0001"))
]
public ItemId parmFormulaItemId(ItemId _formulaItemId = formulaItemId)
{
    formulaItemId = _formulaItemId;
    return formulaItemId;
}
